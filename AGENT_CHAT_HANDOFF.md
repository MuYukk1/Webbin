# Webbin 知识库对话 Agent：执行交接

日期：2026-09-13  
状态：需求与设计交接，尚未实施。本文中的建议默认值可以在实现时调整，但不得放宽用户的免费使用边界。

## 1. 用户目标与已确认约束

在网页上的 Tampermonkey 油猴面板中增加对话功能，根据知识库资料问答。AI 通过工具自主搜索、浏览和分段读取资料，不把所选资料全文一次性加入上下文。

用户明确要求：

- 知识库支持分组；对话支持单选、多选、全选分组，也支持单选、多选、全选具体资料条目。
- 用户使用的模型均支持 tool use；只实现原生工具调用，不做 ReAct 文本 JSON 协议或模型兼容降级。
- 对话运行入口是网页油猴面板。PC 归档脚本未来会退出正式项目，本次不纳入实现范围。
- Cloudflare 必须使用免费能力；对有免费额度限制的操作必须提供限制手段。
- 尽量少改整体架构，优先保留 Worker + KV + 油猴脚本。

“选择对话”在前期讨论中暂按选择知识库条目处理；本次不实现把其他新建问答会话作为可检索知识源。新建问答历史与收藏条目使用不同的数据概念。

## 2. 项目范围及已知事实

正式实现入口：

- `webbin/index.js`：Worker 路由、KV 存储和 AI 总结代理。
- `webbin/userscript/webbin.user.js`：油猴面板、收藏列表、详情和请求封装。
- `webbin/README.md`：安装部署及 API 说明。

路径以工作区 `D:/Project/Tools/Webbin` 为基准。执行前读取适用的项目规则，检查当前 git 状态和部署入口，避免覆盖用户已有改动。下列证据来自上一轮只读检查，行号可能随代码变化：

| 位置 | 已知行为 | 对本次工作的影响 |
| --- | --- | --- |
| `webbin/index.js:48`，`callLLM` | 固定 system/user 消息，只提取文本 | 新聊天流程必须保留完整 assistant 消息及 tool_calls，不能把无文本的工具调用当成空响应错误 |
| `webbin/index.js:74`，`listItems` | 遍历所有 `item:` key | 避免每次 Agent 搜索重新遍历全库 |
| `webbin/index.js:135`，`GET /api/items` | 逐条 KV.get；默认不返回正文或总结文本，full=1 返回整条 | 需要有界的检索元数据获取方式，不能依赖 full=1 全库加载 |
| `webbin/index.js:164` 附近 | 单条读取接口 | 可复用为按需读取的基础 |
| `webbin/index.js:176–208` 附近 | 总结、状态更新为整条记录读改写 | 新分组写入须考虑已有字段丢失与并发覆盖风险 |
| `webbin/userscript/webbin.user.js:59`，`gmFetch` | GM_xmlhttpRequest，120 秒超时 | 新增取消、错误分类等能力时兼容现有调用 |
| `webbin/userscript/webbin.user.js:704` 附近 | 切换 Tab 重建 DOM | Agent 状态不能绑在会被销毁的 DOM 中 |

禁止范围：

- 不修改、迁移或删除 `bili-inbox/`，尤其不要改其中旧 Worker 来实现新功能。
- 不增加对电脑本地归档目录的依赖，不同步 PC 归档数据。
- 不读取或输出真实密钥，不把源码或资料上传到无关服务。
- 本交接不授权生产部署、付费计划升级或额外收费服务开通。

已存云端正文、总结是本功能的数据来源。某条资料没有正文时，必须返回“仅有总结”或“无可读内容”，不得假定本地字幕或归档正文可用。

## 3. 建议实现架构

保留现有三个组成部分：

1. 油猴：分组与范围选择、会话状态、本地检索缓存、Agent 循环、工具执行及过程展示。
2. Worker：认证、模型请求代理、资料与分组读写、有界响应和参数校验。
3. KV：沿用条目存储，保存分组定义；首版不把问答消息逐条写入 KV。

第一版不引入 D1、Vectorize、Durable Objects、后台任务框架或通用 Agent 框架。若实现中发现这些约束无法共同满足，应提交具体冲突与最小替代方案，不自行扩大架构。

Agent 流程：

用户发送问题 → 固定本轮资料范围 → 发送必要历史与工具定义 → 接收原生工具调用 → 校验工具及范围 → 执行工具 → 追加工具结果 → 继续模型调用 → 展示带引用回答。

模型 API key 继续留在 Worker。模型只看到工具定义和选定的资料结果，不接收访问 Token、任意 URL 请求能力或存储管理权限。

## 4. 分组及资料范围

建议每个条目只归属一个分组，增加稳定 `group_id`，分组名称独立存储。旧条目缺少该字段时视为默认分组，不进行全量迁移写入。

分组支持创建、改名、删除和批量移动资料。分组改名不逐条重写资料。默认分组不可删除；删除普通分组需明确资料去向，可将失效 group_id 按默认组解释以避免批量重写，但必须在筛选和显示中保持一致。

提供两种互斥的范围选择方式，避免分组和条目同时勾选时的并集/交集歧义：

- 按分组：单选、多选、全选。
- 按资料：单选、多选、全选；列表选中后可进入“就这些聊”。

空选择禁止发送，不得默认扩大到全库。“全选”作用于当前已完整确定的选择范围；若列表仅加载一页，不得把“本页全选”伪装成“全库全选”。

本轮运行期间锁定范围。范围变更仅对下一轮生效；为避免旧范围资料残留进入新请求，最简单的首版行为是切换范围时开始新会话，并清楚提示。

## 5. 检索与读取

首版在油猴端执行检索，Worker 提供有界的标题、来源、分组、总结等检索信息。不要新增一个随每次保存/总结而改写的全库 `kb:index` key。

索引按条目缓存。首次加载、手动刷新和过期刷新应有分页、条目上限及请求预算；未加载完整时展示覆盖范围，不能暗示全库已检索。不要每轮工具调用刷新索引，也不要使用 `full=1` 拉取整库正文。

实际检索覆盖范围必须透明：

- 基础搜索覆盖已加载的标题和总结。
- 可以附加搜索已缓存正文，但必须标记正文覆盖不完整。
- 无命中不等于所有原文都不存在答案，模型应能浏览条目再按需读取。
- 不承诺未经验证的“90% 命中率”。

工具建议：

| 工具 | 参数示例 | 返回内容 |
| --- | --- | --- |
| `list_items` | cursor、limit | 范围内条目的稳定 ID、标题、来源、正文/总结可用状态，及下一页游标 |
| `search_kb` | query、limit | 有界命中列表、匹配片段、检索字段及覆盖状态 |
| `read_item` | item_id、cursor、limit | 正文或总结片段、资料版本、可定位的片段标识、是否截断及下一段游标 |

范围由应用注入，模型不得通过工具参数扩大范围。每次工具执行校验名称、参数类型、ID、范围和预算。Worker 校验请求参数及有界读取范围；模型没有可绕过客户端工具调度的任意请求工具。

正文首次取得后可在油猴缓存，后续分段不重复读取 KV。注意：只截断传给模型的文本，并不减少 KV 读取整条 value 的代价。单条正文最大大小和缓存容量也要限制。

引用必须来自实际读取/搜索返回的稳定 ID 与片段标识，点击后在面板展示来源。回答区按安全文本/Markdown 渲染，禁止将资料内容当 HTML 直接插入。

## 6. 原生工具调用与会话状态

沿用现有 OpenAI 兼容 Chat Completions 接口形态；添加独立聊天调用路径，避免破坏现有总结逻辑。

- 保留 assistant.tool_calls、调用 ID 和相应 tool 消息，处理同一响应的多个工具调用。
- 模型没有调用工具时允许结束回答，不能据此认定模型不支持 tool use。
- tools 参数被上游拒绝时明确报错，不自动切换文本协议或普通 RAG。
- 工具结果超限时返回可继续读取的结构，不能静默丢掉尾部后假装全文已读。
- 资料文本是引用内容，不是系统指令；不允许从资料中扩展工具白名单或权限。
- 错误、取消和达到预算上限都进入明确终态，不无限重试。

建议集中配置的起始默认值：最多 8 次模型请求、16 次工具执行、单次搜索 10 条、单段读取最多 6000 字符、单轮累计工具正文最多 24000 字符。它们是工程建议值，不是用户指定值，也不是精确 token 计量。须额外限制完整请求大小和历史长度；到上限后提示用户缩小范围或继续新一轮。

会话状态独立于 DOM。切换 Tab 或收起面板不丢消息、不启动重复循环。刷新或关闭宿主网页会中断运行，不承诺后台继续；本地保存可恢复历史及标记中断状态，不能自动重发产生重复费用。

本地持久化优先考虑现有 GM 存储能力，不默认依赖宿主网站 origin 的 localStorage/IndexedDB。设置容量上限、清除入口和多标签页冲突策略。

首版可不做 SSE。显示“搜索资料”“读取某条资料”“生成回答”等真实动作即可，不展示或要求模型内部思维链。

## 7. 免费额度：硬约束与不可混淆的边界

用户要求免费使用且有限制手段。必须区分：

1. 防止收费：只使用 Free 计划和免费资源，不开付费服务或自动升级路径；耗尽额度即停止。
2. 防止额度耗尽：需要覆盖所有设备、页面和访问入口的可靠统一计量，单纯前端限流不能保证。

前一轮讨论提出了“免费计划硬上限 + 客户端预算 + 达限即停”的最小架构方案，用户尚未明确接受它等价于“绝不耗尽免费额度”。执行时不得把两者混为一谈或宣传已实现全账户精确预算。

实施要求：

- 核验部署目标实际计划、最新官方额度与达限行为；不能只根据仓库说明推断账户处于 Free。
- 未能确认计划时，完成本地代码与测试，将免费计划验证列为上线前阻塞项；不擅自部署。
- 不创建用 KV 每请求读改写的全局计数器；它本身消耗额度，且不能可靠并发计数。
- 所有批量操作、索引加载及 Agent 读取均有可见的数量上限和请求预算。
- 同条目写入串行化，减少重复写；本地队列只控制本页面，不能宣称解决跨设备 KV 并发一致性。
- 限流/额度错误触发停止或有界退避，不无限重试、不自动扫库重建缓存。
- 区分认证失败、网络错误、上游模型限流和 CF 配额错误，不把所有错误都解释为免费额度耗尽。
- 缓存过期、删除、分组变更时失效对应条目；读取不到或范围验证失败时停止使用旧缓存作当前证据。
- 单次请求必须限制请求体、messages 总大小、工具结果大小和服务器侧批量操作数量。

如果用户最终要求“在任何设备并发下均能准确预留每日额度，永不触及 CF 硬上限”，需要重新评估统一计量方案；在当前最小架构下不得伪造保证。先完成不依赖该决策的工作，提交具体差异供用户选择。

模型供应商费用与 CF 免费额度分开。模型请求次数、请求大小可控，但不得称模型 API 免费，也不得自动调用其他模型供应商。

## 8. 实施顺序

1. 核对当前代码、规则和部署入口，保留既有行为；确认分组数据与缓存失效方式。
2. 完成分组 CRUD、批量移动与资料范围选择，处理旧条目兼容。
3. 提供有界检索元数据加载和按需读取；完成本地缓存与三个只读工具。
4. 增加聊天代理、工具循环、预算控制、停止功能和来源引用。
5. 完成会话本地保存、Tab 切换恢复、异常状态和免费限制说明。
6. 运行针对性测试、面板交互验证，更新 README，提交实现摘要和待验证项。

不要求为了本次功能重构全项目，不顺带清理 PC 代码、不迁移全部存储。

## 9. 验收标准

功能：

- 旧条目进入默认分组，原有保存、列表、详情和总结功能保持可用。
- 分组/资料单选、多选、全选与实际检索范围一致，空选不会变成全库。
- 查询首轮不包含全库正文；工具可完成搜索 → 读取 → 回答，多次读取可定位后续段落。
- 只有总结的资料明确标识，不捏造正文；答案引用可回到真实资料。
- 工具请求其他范围 ID、非法工具名、异常参数会被拒绝。

额度与可靠性：

- 重复搜索不重复拉取索引，重复分段读取命中缓存；测试记录实际请求次数。
- 索引分页、未完整覆盖、批量写入及超预算均有明确状态。
- 单次多个 tool_calls、空文本工具响应、工具失败、上游超时和取消均能正确结束或继续。
- 达到请求/读取/内容预算后没有额外无界调用；CF 达限时停止，不反复重试。
- Tab 切换、收起、刷新、多页面同时操作有明确行为；不宣称本地队列解决全局并发。
- 测试提示注入资料不能触发写入、删除、任意外网访问或泄露配置。

验证方式：优先使用模拟 KV、模拟模型响应验证真实边界，沿用项目已有测试方式。真实 API 验证控制次数和资料范围；未经授权不启动大规模费用测试。桌面与安卓油猴环境未实际验证的部分必须标注，不能用桌面测试代替双端验证结论。

交付：提供修改文件、行为说明、测试结果、免费额度保护的实际边界以及上线前未解决事项。不要只交“实现完成”而省略部署计划或配额未验证的限制。

## 10. 官方资料与后续核验

以下来源在 2026-09-12 调研时核验过，实施时以最新官方文档与目标账户实际计划为准：

- KV 写入、同 key 频率与并发覆盖：https://developers.cloudflare.com/kv/api/write-key-value-pairs/
- KV 免费额度：https://developers.cloudflare.com/kv/platform/pricing/
- KV 限制：https://developers.cloudflare.com/kv/platform/limits/
- Workers 限制：https://developers.cloudflare.com/workers/platform/limits/
- Tampermonkey 请求及存储 API：https://www.tampermonkey.net/documentation.php

已知注意点：Free Worker 的 CPU 时间与网络等待不是同一概念；外部 subrequest 与 CF 服务调用限制应分别核验。不要照搬“所有子请求总共 50 次”的旧表述。

如以后评估新存储：Vectorize 专属定价页已有 Free 配额，不能以“必需付费”为由排除；D1 支持 FTS5，但本次首版不引入两者。
